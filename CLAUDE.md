- Always keep the intergrity our project, never make changes that will damage what we have built.
- If i ask for the training script i will simple do /training and you response should be -   # Train on all approved entries
  python3 scripts/build_training_dataset.py

  # Train on entries from last 30 days only
  python3 scripts/build_training_dataset.py --days 30